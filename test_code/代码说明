🧠 1. Self-Attention 策略
• TransformerEncoder 用于建模候选之间的相互影响；
• 输入：候选特征 [B, K, D]；
• 输出：每个候选的嵌入；
• 用 Linear 层将每个候选映射为打分；
• 对候选 mask 做 masked softmax，得到概率分布；
• 采样动作，计算 log_prob 和 entropy。
💎 2. PPO 更新
• 利用 GAE 计算优势；
• 计算 PPO clipped loss；
• 包含 actor、critic、entropy 三部分；
• 支持变长候选的 padding + mask。
⚙️ 3. 环境
• 每步动作数量不同；
• 奖励随机；
• 主要用于演示可变动作数结构的稳定训练。